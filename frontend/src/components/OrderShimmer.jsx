import { ORDER_SHIMMER_IMG } from "../utils/constants";

const OrderShimmer = () => {
  return (
    <div className="flex items-center justify-center">
      <img src={ORDER_SHIMMER_IMG} className="w-[600px]" />
    </div>
  );
};

export default OrderShimmer;
